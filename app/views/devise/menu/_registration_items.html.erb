<% if user_signed_in? %>
  <li>
  <%= link_to('My Account', send("edit_#{current_user.role.underscore}_registration_path", current_user)) %>
  </li>
<% else %>
  <li>
  <%= link_to('Register', new_submitter_registration_path)  %>
  </li>
<% end %>