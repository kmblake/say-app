<div class="jumbotron">
  <h1 class="center"><%= @document.title %> <span class="weak"> (<%= @document.style %>)</span></h1>
  <p>This is how your submission will appear to the SAY editors.  If you cannot see it here, unfortunately we won't be able to see it either!</p>
</div>

<%= link_to 'Edit Submission Info', edit_document_path(@document), class: ' bottom-margin btn btn-primary' %>
<%= link_to 'Remove Submission', @document, method: :delete, class: 'btn btn-danger bottom-margin right' %> 

<iframe src="https://docs.google.com/viewer?url=<%= URI(request.url) + @document.file.url %>&embedded=true" 
width="100%" height="800" frameborder="0" >
<p>Your browser does not support iframes.</p>
</iframe>


